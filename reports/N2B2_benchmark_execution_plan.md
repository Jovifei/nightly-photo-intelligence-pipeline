# N2B2 Benchmark Execution Plan (Locked)

N2B2 is locked. Only after separate artifact approval may it load one model at a time, run three synthetic smoke fixtures, then the approved 20-item G1 snapshot serially, and record resource measurements, OOM/failure causes, pose/keypoint quality and segmentation mask quality. It may not enter G2 or retain unapproved derivatives.
