# N2B0 Owner Decision Packet

Option A: approve only a future N2B1 qualification for the exact Pose and segmentation records after their missing immutable revision, byte-size, weight-license, and hash fields are supplied; no inference.

Option B: approve only Pose N2B1 after the MMPose checkpoint evidence is complete; keep segmentation locked.

Option C: keep all downloads locked. This is the recommended current option because both artifact records are `INCONCLUSIVE`.

Current disposition: `N2B0_ARTIFACT_QUALIFICATION_BLOCKED`. The RTMW fallback metadata typo is corrected and its official URL returned HEAD 200, but no option authorizes a download or inference; Option A/B still require the missing weight-license, immutable-revision, and SHA-256 evidence.
