# Contributing

第一版由 Owner 和受控智能体在私有独立仓库中开发。

- 先验证 `PROJECT_STATE.json`；
- 只做当前任务；
- 每个行为有测试；
- 不提交运行数据；
- 不改变 Gate；
- 不从主 App 复制业务源码；
- 不加入网络/云 fallback；
- 外部代码必须登记来源/许可；
- 阶段完成后停止。

N0 要求一个 isolated commit；后续 Git 流程由 Owner 另行批准。
