# N2B Model Download Request (Not Authorized)

This document is an approval package draft only. N2B remains LOCKED and no
artifact may be downloaded from this file.

Each candidate request must be approved separately with:

1. model/project ID and exact immutable revision;
2. exact artifact filename, expected byte size, and official source URL;
3. code license, checkpoint/weights license, training-data terms, and commercial restrictions;
4. download destination outside Git and outside the source-photo tree;
5. SHA-256 acquisition and verification procedure;
6. runtime/dependency versions and whether Docker/WSL is required;
7. input dimensions, expected memory, and VRAM fields marked `TBD_PENDING_REAL_BENCHMARK`;
8. the approved synthetic/owner-provided 20-case benchmark manifest;
9. deletion and rollback procedure;
10. Owner approval and independent review.

Current requests: none approved. All CSV entries are `DOWNLOAD_NOT_AUTHORIZED`.
