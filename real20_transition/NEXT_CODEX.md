# R0 local acceptance -> R1 real implementation -> R2 bounded evaluation

## 0. Owner intent and scope

Jovi has repeatedly authorized advancing to Real20 and now requests cloud changes
and local receipt. Do not ask again whether to start working on Real20. The next
work is concrete code/preparation plus a new, exactly bound data/execution record,
not reuse of H3 or expired G1 credentials. Do not label another recap as execution.
Code/testing/normal push on this transition branch is within the requested work.
Actual data access must occur only through a newly valid, reviewed, scope-bound
Real20 entry point; no helper may manufacture APPROVED status as a shortcut.

Use the existing current environment. No model replacement, global Python changes,
Ollama update, Developer Mode, ACL edit, or unapproved wheel installation.

## 1. Fetch, protect, and inspect

Fetch current refs/tags, inspect newer changes rather than reset them. Reviewed H3
base is ffc4130823c1308f089b835c766e341ec2173e82, tree
3b01e26388d131181a8553b4122a9abbaff177b2. R0 is on
`gpt/real20-transition-20260921`, not an old synthetic/H0 branch.
Use new ordinary-user clean worktrees; primary AGENTS must remain 2638 bytes and
b07a0460c28c74388b4d83500b128aa59fc8af1af9a753e97e1a5f8bdea70048 unless Jovi
has explicitly made another change; never overwrite a newer user edit to match it.

Read README, STATE, prepare.py and tests. Run verify_delivery and tests using the
real existing Python 3.12 interpreter. Record tool versus H3 runtime identities
separately. Do not bind the R0 tool's SHA as the H3 runtime SHA.

## 2. Reconcile H3 ONCE, do not rerun it

Locate only known control files for the SUCCESSFUL lease:
6763dcfe658860a1c0ea76689799d5e0bd92d7d51c9898a47f75c8cee45a99e4.
Review artifact expected SHA:
820f76ba48c5d55fd66b83f158afe7b12b62aad0315df710a57163ea407ffc33.
Preserve the previous FAILED lease and the failed torch-download history.

Run prepare.py with all explicit paths from --help, reading control files only.
Do not scan photos; the source-root argument is solely an exclusion boundary.
Use exact existing G1 manifest SHA:
29eee5fdb8e16c85649752501c4b722d2b4d071950c8a989add040e895a75c47.
A hash mismatch is an input-binding problem: don't edit constants to make it pass.

Compare two NEW output files byte-for-byte, strict-parse the whole JSON documents,
compute hashes by program, and keep all metadata outputs outside Git. A literal
backslash+n appended to JSON is invalid and must not be ignored as trailing data.
The checker validates selected consistency, not a fresh independent Review.
Read the actual original reviewer report; retain the scope of its verdict.

Record the H3 closeout and one consolidated R0 blockers table. The code/source
missing from current main is an implementation item, not another request for the
same generic Owner yes/no. Read all blockers in one pass, not one per conversation.

## 3. Complete R1 in this same code-working session

Read the actual local Git objects for historical 49ebb1c/72bd862 if they exist.
The cloud could not resolve 49ebb1c remotely. Never claim it was ported unless read.
If available, preserve it as historical evidence and selectively port useful
metadata/path/manifest logic. Do not merge old approvals, old phase unlocks or
old candidate identities into current main. If unavailable, implement the bounded
entry using current source guards/leases/worker architecture; do not block on
restoring an unavailable historical commit.

Required actual implementation:

- Versioned `real20 prepare` and `real20 run` interfaces, distinct from synthetic.
  Preparation reads control metadata only and reports all missing runtime/data/code
  bindings. Run must reject with no file reads until the exact Real20 permit is valid.
- A narrow REAL20 evaluation task contract with production N2B2 still LOCKED and
  no N3/N4/N5 general unlock. Separate data manifest/readonly snapshot authority from
  candidate/runtime/model/expiry authority. Old G1 and synthetic permits never satisfy it.
- Bind the FINAL implementation candidate/tree/full source/quality record and H3
  provenance; do not call ffc4130 the execution candidate after code changes.
- Exact 20-entry manifest, 19 canonical images and one reference after local
  recomputation. No 21st/off-manifest access and no directory discovery.
- Existing OS read-only/bound-handle guard before opening each original, recheck
  identities/digests and permission evidence. No source mutation or ACL repair.
- Non-sensitive EXIF allowlist from R0; discard GPS, capture time, serials,
  Owner/Artist/UserComment/MakerNote. Never copy raw EXIF into public reports.
- Existing Pose/Seg/Qwen model stack and fact v1.2, one heavy role at a time,
  immutable facts versus interpretations separated. Synthetic fixtures are not
  relabeled real and real photos are not relabeled synthetic to bypass checks.
- Output evaluation records/diagnostics/checksums in new external locations,
  no production Bundle, no SQLite writes, no App writes. Per-case errors are
  truthful; never replace failed model output with an approval or a passing zero.
- Real one-shot reservation and terminal result; exceptions retain diagnostics
  and post-run readonly/integrity checks. A failed attempt consumes its allowance.
- A supported WORKER interpreter with torch/torchvision/CUDA/operators verified
  before reservation, not merely a test interpreter with pytest. Metadata-only
  R0 checks are the first check, not full CUDA readiness evidence. Keep environment
  identity visible; do not switch to unsupported 3.14 to use installed Torch.

## 4. Test the default path, then publish code

Use temporary synthetic image inputs (never real photos yet), actual CLI parsing,
actual data/phase gate, source guard, duplicate planning and ledger, with model
backend replaced only where necessary. Cover a normal accepted test-run and:
expired lease, old synthetic/G1 lease, wrong candidate/manifest/source fingerprint,
21st entry, duplicate metadata conflict, changed source bytes, ADS/junction/reparse,
root replacement, missing Torch/operator capability, worker failure, duplicate
lease consumption, and source checks on both success and failure. No fake PASS.

Run actual supported Python 3.12 project quality: pytest, run_quality, handoff,
preflight, Ruff/format, mypy, schema, sensitive scan, git diff --check. Set and
record PYTHONPATH=src when using a non-editable src checkout. Record exact skipped
capabilities and native winerror values; only genuine 1314 can be that symlink gap.

Rebuild root MANIFEST from final Git index, register a bounded code-only successor
without weakening protected state/tag/approval checks, normal commit/push to the
transition branch, and read the remote SHA back. Do not change main automatically.
Do not put raw data, absolute local paths, plans with filenames, model payloads,
logs, local approvals or raw runtime files in Git. A brief sanitized status report
may contain computed SHAs and tested scope. Keep all old H3 evidence unchanged.

## 5. Materialize the next operation, not another generic authorization loop

With code and local evidence complete, issue one exact execution worksheet:
final code SHA/tree, frozen 20-asset manifest and source fingerprint, readonly
capability, model/environment/runtime identity, outputs, allowed EXIF, one-run
limit, exact UTC validity window, retained production lock, and exclusions.
Use Jovi's already-recorded Real20 intent as the requested scope. Ask only about
an actual missing decision or changed scope, not another bare 'authorize?'.
A requested validity window may be proposed as 24 hours beginning at the next
approved start; do not backdate it or choose an already-expired window.
Actual APPROVED data/lease/Owner-anchor artifacts must be materialized through
the project's Owner mechanism after final bindings exist. R0 itself cannot issue
or consume them. Do not start data reads merely because code was pushed.

An independent code review must see the FINAL actual default path before R2.
If all required authorization details are not yet materialized, finish code and
send the single complete worksheet; do not stop with only an abstract DRAFT.

## 6. R2 and product direction

When the exact new record is valid, one Real20 evaluation, then stop at
REAL20_COMPLETE_AWAITING_PHOTOGRAPHY_REVIEW (or an honest failed state).
Human review measures correct person count/pose/segmentation/composition,
unsupported interpretations, usefulness of advice, editing time and decision.
No machine-generated APPROVED labels. Only later, separately qualified export
produces an APPROVED-only Bundle for the independent App import/display/reject/
rollback test. Do not add a model/vector-DB/OpenClaw detour.

## Final reply must distinguish done from not done

Report R0 hashes and selected H3 checks; R1 actual executable call chain and code
SHA/parent/tree/remote; real quality matrix/native gaps; exact remaining blockers;
original AGENTS/state/source/evidence unchanged proof; one consolidated next-run
worksheet. Mark explicitly whether real data was read (it must be NO during R0/R1).
No new inference result may be attributed to an authorization given after that
result was already produced. No artifact or test count may be guessed.
