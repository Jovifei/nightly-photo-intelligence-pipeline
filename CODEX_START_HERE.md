# Codex Start Here

Codex 不是产品范围所有者。当前唯一任务是 N2B0.7 GPU-native artifact qualification。

```bash
python tools/verify_handoff.py
```

然后严格按：

```text
docs/00_reading_order.md
tasks/phase_n2b0_7_gpu_native_qualification.yaml
```

执行。不得下载或 Range 请求模型、不得安装/运行模型、不得接触真实照片、不得修改主 App、不得接入 OpenClaw。完成一个 N2B0.7 本地资格审查 commit 后，输出主合同中的停止消息并等待 Owner artifact decision。
