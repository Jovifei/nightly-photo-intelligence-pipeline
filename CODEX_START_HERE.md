# Codex Start Here

Codex 不是产品范围所有者。当前唯一任务是 N2B0.6 官方元数据研究。

```bash
python tools/verify_handoff.py
```

然后严格按：

```text
docs/00_reading_order.md
tasks/phase_n2b0_6_license_clear_alternative_candidate_research.yaml
```

执行。不得下载或 Range 请求模型、不得安装/运行模型、不得接触真实照片、不得修改主 App、不得接入 OpenClaw。维护唯一未批准的 N2B0.6 候选后，输出主合同中的停止消息并等待独立审查。
