# Codex Start Here

Codex 不是产品范围所有者。当前唯一任务是 N0。

```bash
python tools/verify_handoff.py
```

然后严格按：

```text
docs/00_reading_order.md
tasks/phase_n0_environment_scaffold.yaml
```

执行。不得下载模型、不得接触真实照片、不得修改主 App、不得接入 OpenClaw。完成一个 commit 后输出主合同中的停止消息并停止。
