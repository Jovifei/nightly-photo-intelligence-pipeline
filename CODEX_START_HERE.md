# Codex Start Here

Codex 不是产品范围所有者。当前唯一任务是 N2B1R local-research model acquisition。

```bash
python tools/verify_handoff.py
```

然后严格按：

```text
docs/00_reading_order.md
tasks/phase_n2b1r_local_research_model_acquisition.yaml
```

执行。仅可按 owner-approved register 下载到 Git 外 quarantine，并在 Git 外隔离环境安装 hash-locked wheel；不得加载或运行模型、不得接触真实照片、不得修改主 App、不得接入 OpenClaw。完成一个 N2B1R 本地 acquisition commit 后，验证下一阶段 gate，绝不自动进入真实 benchmark。
