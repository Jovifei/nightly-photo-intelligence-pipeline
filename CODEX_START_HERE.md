# Codex Start Here

Codex 不是产品范围所有者。当前唯一任务是 N2B1P local-research cache promotion。

```bash
python tools/verify_handoff.py
```

然后严格按：

```text
docs/00_reading_order.md
tasks/phase_n2b1p_local_research_cache_promotion.yaml
```

执行。仅可按 Owner-approved N2B1R evidence 将三份 payload 从 Git 外 quarantine 复制到 Git 外内容寻址 cache；不得下载、安装、加载或运行模型，不得接触真实照片、不得修改主 App、不得接入 OpenClaw。完成一个 N2B1P 本地 promotion commit 后，验证下一阶段 gate，绝不自动进入真实 benchmark。
