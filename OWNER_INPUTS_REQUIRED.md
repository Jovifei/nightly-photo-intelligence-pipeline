# Owner 需要提供或批准的输入

缺失输入不能由智能体猜测。N0 可在缺失时记录并继续安全范围内工作；涉及高风险动作时必须停止。

## N0 前或 N0 中

| 输入 | 是否阻塞 N0 | 说明 |
|---|---:|---|
| 独立工程目录 | 是 | 不得位于主 App 仓库内部 |
| 允许初始化 Git | 是 | 仅本地 `git init` 与一个 commit |
| Python 3.11/3.12 现状 | 否 | 缺失则报告，不安装系统 Python |
| 三张合成 fixture | 已提供 | 位于 `fixtures/three_image_smoke_set/` |
| 允许本地质量依赖的现状 | 否 | N0 不联网安装；缺失标记 `NOT_AVAILABLE` |
| 目标项目许可证 | 否 | 未决定时不要猜；可保持私有 |
| 报告中的机器名脱敏规则 | 否 | 默认统一写 `<HOST_REDACTED>` |

## N1 前必须明确

- 真实照片根目录的 Owner 授权；
- Windows/WSL 中只读访问方式；
- 工作目录与输出磁盘预算；
- 20 张校准清单，不是整个目录；
- 是否允许读取真实 EXIF；读取不等于推断；
- 文件扩展名白名单；
- 原图目录备份状态；
- 失败和恢复的夜间窗口。

## 任何模型下载前

必须有单独批准文件，包含：

- 候选名称和用途；
- 精确仓库/模型卡；
- exact revision；
- 文件清单、预估大小和 SHA-256；
- 代码许可、权重许可、训练数据已知限制；
- 下载目标目录；
- 网络域名白名单；
- 删除/回滚方式；
- 批准的数据门禁和有效期。

## 20 / 100 / 全量前

每个放量都需要：

- 数据 manifest；
- Owner 签署；
- 上一 Gate 证据；
- 预计时间、磁盘和显存；
- 可恢复演练；
- 停机阈值；
- 人工抽检计划；
- 确认仍不上传云端。

## OpenClaw 前

除 N8 通过外，还必须签署独立激活批准。任何“已经安装 OpenClaw”不等于批准它访问本项目。
